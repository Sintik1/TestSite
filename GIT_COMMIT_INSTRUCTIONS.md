# Инструкция: Коммит новых файлов в Git

## ⚠️ Проблема

Jenkins не может найти класс `BaseTest` при компиляции, потому что файл `BaseTest.java` не был закоммичен в Git репозиторий.

## ✅ Решение

Нужно закоммитить новый файл `BaseTest.java` в Git репозиторий.

### Шаги:

1. **Проверьте статус Git:**
   ```bash
   git status
   ```
   Должен показать `BaseTest.java` как неотслеживаемый файл (untracked).

2. **Добавьте файл в Git:**
   ```bash
   git add src/test/java/BaseTest.java
   ```

3. **Закоммитьте изменения:**
   ```bash
   git commit -m "Add BaseTest class for test inheritance and driver management"
   ```

4. **Отправьте изменения в репозиторий:**
   ```bash
   git push origin main
   ```
   (или `git push origin master` если ваша ветка называется master)

### Альтернативно: добавить все измененные файлы

Если нужно закоммитить все изменения сразу:

```bash
# Добавить все измененные и новые файлы
git add .

# Закоммитить
git commit -m "Fix: Add BaseTest, update PageAuth, add assertions to tests"

# Отправить
git push origin main
```

## 📋 Файлы, которые нужно закоммитить:

- ✅ `src/test/java/BaseTest.java` - **НОВЫЙ файл (обязательно!)**
- ✅ `src/test/java/TestPageAuthorization.java` - изменен
- ✅ `src/main/java/org/example/PageAuth.java` - изменен
- ✅ `pom.xml` - изменен

## 🔍 Проверка

После push в Jenkins:
1. Jenkins автоматически запустит сборку (если настроен webhook)
2. Или запустите сборку вручную
3. Проверьте, что компиляция проходит успешно
4. Тесты должны запуститься

## ⚠️ Важно

Если файл `BaseTest.java` не будет в Git репозитории, Jenkins всегда будет падать с ошибкой компиляции:
```
cannot find symbol: class BaseTest
```

---

**После выполнения этих шагов проблема должна быть решена!**
